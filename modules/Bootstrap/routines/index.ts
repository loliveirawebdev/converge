import moveForward from "./moveForward";
import onError from "./onError";

// the order does not matter here
// to configure the flow, take a look at config/flow.json
export default [moveForward, onError];
